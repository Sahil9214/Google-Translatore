// This is a placeholder file to maintain imports
// MongoDB functionality has been removed from the application

console.log("MongoDB functionality has been disabled");

// Dummy client promise that does nothing
const clientPromise = Promise.resolve(null);

export default clientPromise;
